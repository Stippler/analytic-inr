torch>=2.0.0
numpy>=1.20.0
matplotlib>=3.5.0
scipy>=1.7.0
trimesh>=3.9.0
click>=8.0.0
# Optional: pysdf>=0.1.9 (for faster SDF queries, but trimesh has built-in proximity queries)
# Optional: point-cloud-utils (for faster batched SDF queries)

